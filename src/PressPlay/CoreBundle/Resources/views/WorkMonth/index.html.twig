<h1>WorkMonth list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Month</th>
            <th>Year</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_workmonth_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.month }}</td>
            <td>{{ entity.year }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_workmonth_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_workmonth_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_workmonth_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
