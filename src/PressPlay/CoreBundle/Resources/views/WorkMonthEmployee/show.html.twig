<h1>WorkMonthEmployee</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Workhours</th>
            <td>{{ entity.workhours }}</td>
        </tr>
        <tr>
            <th>Workdays</th>
            <td>{{ entity.workdays }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('workmonthemployee') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('workmonthemployee_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('workmonthemployee_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
